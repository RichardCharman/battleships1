require 'board'

describe Board do
  
  it {is_expected.to respond_to :size}
    
 #    it 'raise an error if outside of board constraint' do
 #    expect {subject.size}.to raise_error 'Out of bounds'
	# end
end